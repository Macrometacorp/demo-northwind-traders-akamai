const Button = {
  baseStyle: {},
};

const Input = {
  parts: ["field"],
  defaultProps: {
    focusBorderColor: "primary.400",
  },
  sizes: {
    sm: {
      field: {
        borderRadius: "md",
      },
    },
  },
};

const Select = {
  parts: ["field"],
  defaultProps: {
    focusBorderColor: "primary.400",
  },
  sizes: {
    sm: {
      field: {
        borderRadius: "md",
      },
    },
  },
};

export const components = {
  Button,
  Input,
  Select,
};
